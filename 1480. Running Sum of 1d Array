class Solution(object):
    def runningSum(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        ln = len(nums)
        ans = []
        for i in range(1, ln+1):
            ans.append(sum(nums[:i]))
        return ans
